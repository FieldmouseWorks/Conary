{
	"name": "conary-site",
	"version": "0.1.0",
	"private": true,
	"type": "module",
	"scripts": {
		"predev": "bash ../scripts/sync-brand.sh",
		"dev": "vite dev",
		"prebuild": "bash ../scripts/sync-brand.sh",
		"build": "vite build && npm run verify:build",
		"preview": "vite preview",
		"precheck": "bash ../scripts/sync-brand.sh",
		"check": "svelte-kit sync && svelte-check --tsconfig ./tsconfig.json && npm test",
		"verify:build": "node scripts/verify-build.mjs",
		"test": "vitest run"
	},
	"devDependencies": {
		"@sveltejs/adapter-static": "^3.0.8",
		"@sveltejs/kit": "^2.70.3",
		"@sveltejs/vite-plugin-svelte": "^7.3.0",
		"@types/js-yaml": "4.0.9",
		"@types/node": "^26.5.0",
		"js-yaml": "5.4.1",
		"svelte": "^5.57.0",
		"svelte-check": "^4.7.6",
		"typescript": "^6.0.3",
		"vite": "^8.2.2",
		"vitest": "^5.0.0"
	},
	"overrides": {
		"cookie": "0.7.2"
	}
}
